# ai-job-scraper-pipeline
Autonomous end-to-end ETL pipeline that automates internship sourcing. Ingests live web data via API, executes contextual GenAI screening using Google Gemini with strict schema constraints, and parses structured JSON. Engineered with an advanced conditional routing logic gate to prevent duplicate rows and optimize downstream token costs
